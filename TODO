- Create a daemon using config file [WIP],
- Allowing multi alerts,
- Check OOM when job is finished,
- Allowing action after job is finished (re-launch, reboot, shutdown, ...),
- Allowing monitoring of resources during the run (eg: RAM every x mircoseconds, ...)
